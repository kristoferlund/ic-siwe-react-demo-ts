// This type is manually copied from the backend to the frontend since Azle does not generate types returned by canister methods.
export type UserProfile = {
  avatar_url: string;
  name: string;
  address: string;
};
